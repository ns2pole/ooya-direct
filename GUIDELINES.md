# 開発方針

1. **TDD で進める** — 先に失敗するテストを書き、実装で通す。UI だけでなくロジックは純関数に切り出してテストしやすくする。
2. **実装後に不要コードを削除する** — 使われなくなったコンポーネント・CSS・import・分岐は都度確認して消す。動くだけ残さない。
3. **重複を共通化してコード量を減らす** — 同じ処理が複数箇所にあるときは lib / コンポーネントにまとめ、呼び出し側を薄く保つ。
